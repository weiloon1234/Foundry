[package]
name = "foundry_fixture_plugin_dep"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
async-trait = "0.1"
foundry = { path = "../../../" }
foundry_fixture_plugin_base = { path = "../plugin_base" }
semver = "1"
